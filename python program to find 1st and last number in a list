def swaplist(newList):
    size = len(newList)
    
    temp = newList[0]
    newList[0] = newList[size - 1]
    newList[size - 1] = temp
    
    return newList
    
n = list(map(int,input().split()))
print(swaplist(n))
    
